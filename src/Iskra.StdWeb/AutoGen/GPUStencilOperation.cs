// <auto-generated/>

namespace Iskra.StdWeb;

#nullable enable

public sealed partial class GPUStencilOperation
{
    private readonly string _value;

    private GPUStencilOperation(string value)
    {
        _value = value;
    }

    public static readonly GPUStencilOperation Keep = new("keep");
    public static readonly GPUStencilOperation Zero = new("zero");
    public static readonly GPUStencilOperation Replace = new("replace");
    public static readonly GPUStencilOperation Invert = new("invert");
    public static readonly GPUStencilOperation Increment_clamp = new("increment-clamp");
    public static readonly GPUStencilOperation Decrement_clamp = new("decrement-clamp");
    public static readonly GPUStencilOperation Increment_wrap = new("increment-wrap");
    public static readonly GPUStencilOperation Decrement_wrap = new("decrement-wrap");

    public override string ToString() => _value;

    public static GPUStencilOperation Create(string value) => value switch
    {
        "keep" => Keep,
        "zero" => Zero,
        "replace" => Replace,
        "invert" => Invert,
        "increment-clamp" => Increment_clamp,
        "decrement-clamp" => Decrement_clamp,
        "increment-wrap" => Increment_wrap,
        "decrement-wrap" => Decrement_wrap,
        _ => throw new ArgumentException($"Invalid value \"{value}\" for GPUStencilOperation", nameof(value)),
    };
}

#nullable disable