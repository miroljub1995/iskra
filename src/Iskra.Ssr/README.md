# Iskra.Ssr

Server-side rendering helpers for Iskra applications hosted on ASP.NET Core.

## Features

- `MainScript` component that resolves the fingerprinted `_framework/dotnet.js` static asset route from endpoint metadata.

## Usage

Reference `Iskra.Ssr` from your server project and use components from `Iskra.Ssr.Components`.
