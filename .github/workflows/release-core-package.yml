name: Release Core Package

on:
  push:
    branches:
      - main

permissions:
  contents: write
  packages: read

concurrency:
  group: release-core-package-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    name: Release Core Family Packages
    runs-on: ubuntu-latest
    env:
      ISKRA_API_VERSION: 0.3.0

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          global-json-file: global.json

      - name: Resolve release version from Core
        id: version
        shell: bash
        run: |
          set -euo pipefail

          version=$(dotnet msbuild src/Iskra.Core/Iskra.Core.csproj -nologo -v:q -getProperty:IskraCoreVersion | tr -d '\r\n')
          if [[ -z "$version" ]]; then
            echo "Failed to resolve Version from src/Iskra.Core/Iskra.Core.csproj"
            exit 1
          fi

          tag="core/v${version}"

          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"

          echo "Resolved version: $version"
          echo "Release tag: $tag"

      - name: Check if GitHub release already exists
        id: release_check
        uses: actions/github-script@v9
        env:
          RELEASE_TAG: ${{ steps.version.outputs.tag }}
        with:
          script: |
            const tag = process.env.RELEASE_TAG;

            try {
              await github.rest.repos.getReleaseByTag({
                owner: context.repo.owner,
                repo: context.repo.repo,
                tag
              });

              core.info(`Release already exists for tag ${tag}. Skipping pipeline.`);
              core.setOutput('exists', 'true');
            } catch (error) {
              if (error.status === 404) {
                core.info(`No release found for tag ${tag}. Continuing.`);
                core.setOutput('exists', 'false');
              } else {
                throw error;
              }
            }

      - name: Stop early because release exists
        if: steps.release_check.outputs.exists == 'true'
        shell: bash
        run: |
          echo "Release ${{ steps.version.outputs.tag }} already exists. Skipping pack and publish steps."

      - name: Pack NuGet packages
        if: steps.release_check.outputs.exists != 'true'
        shell: bash
        run: |
          mkdir -p artifacts/nuget

          dotnet pack src/Iskra.Signals/Iskra.Signals.csproj -c Release -o artifacts/nuget \
            -p:IskraApiVersion=$ISKRA_API_VERSION
          dotnet pack src/Iskra.Core/Iskra.Core.csproj -c Release -o artifacts/nuget \
            -p:IskraApiVersion=$ISKRA_API_VERSION
          dotnet pack src/Iskra.Browser.Abstractions/Iskra.Browser.Abstractions.csproj -c Release -o artifacts/nuget \
            -p:IskraApiVersion=$ISKRA_API_VERSION
          dotnet pack src/Iskra.Browser/Iskra.Browser.csproj -c Release -o artifacts/nuget \
            -p:IskraApiVersion=$ISKRA_API_VERSION
          dotnet pack src/Iskra.Dom/Iskra.Dom.csproj -c Release -o artifacts/nuget \
            -p:IskraApiVersion=$ISKRA_API_VERSION
          dotnet pack src/Iskra.Ssr.Abstractions/Iskra.Ssr.Abstractions.csproj -c Release -o artifacts/nuget \
            -p:IskraApiVersion=$ISKRA_API_VERSION
          dotnet pack src/Iskra.Ssr/Iskra.Ssr.csproj -c Release -o artifacts/nuget \
            -p:IskraApiVersion=$ISKRA_API_VERSION

      - name: Upload package artifacts
        if: steps.release_check.outputs.exists != 'true'
        uses: actions/upload-artifact@v7
        with:
          name: nuget-packages-${{ steps.version.outputs.version }}
          path: artifacts/nuget/*

      - name: Publish packages to NuGet
        if: steps.release_check.outputs.exists != 'true'
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
        shell: bash
        run: |
          set -euo pipefail

          shopt -s nullglob
          packages=(artifacts/nuget/*.nupkg artifacts/nuget/*.snupkg)

          if [[ ${#packages[@]} -eq 0 ]]; then
            echo "No packages found to publish."
            exit 1
          fi

          for package in "${packages[@]}"; do
            dotnet nuget push "$package" \
              --source https://api.nuget.org/v3/index.json \
              --api-key "$NUGET_API_KEY" \
              --skip-duplicate
          done

      - name: Create GitHub release
        if: steps.release_check.outputs.exists != 'true'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: ${{ steps.version.outputs.tag }}
          generate_release_notes: true
          files: |
            artifacts/nuget/*.nupkg
            artifacts/nuget/*.snupkg

      - name: Workflow summary
        shell: bash
        run: |
          {
            echo "## Release Summary"
            echo "- Version: ${{ steps.version.outputs.version }}"
            echo "- Tag: ${{ steps.version.outputs.tag }}"
            echo "- Release exists: ${{ steps.release_check.outputs.exists }}"
          } >> "$GITHUB_STEP_SUMMARY"
