// <auto-generated/>

namespace Iskra.StdWeb;

#nullable enable

public sealed partial class RequestMode
{
    private readonly string _value;

    private RequestMode(string value)
    {
        _value = value;
    }

    public static readonly RequestMode Navigate = new("navigate");
    public static readonly RequestMode Same_origin = new("same-origin");
    public static readonly RequestMode No_cors = new("no-cors");
    public static readonly RequestMode Cors = new("cors");

    public override string ToString() => _value;

    public static RequestMode Create(string value) => value switch
    {
        "navigate" => Navigate,
        "same-origin" => Same_origin,
        "no-cors" => No_cors,
        "cors" => Cors,
        _ => throw new ArgumentException($"Invalid value \"{value}\" for RequestMode", nameof(value)),
    };
}

#nullable disable