# iskra

Missing features:
- Callback interface types. Operation should be handled similar as attribute with delegate type.
- Namespace types. It should be handled similar to Dictionary types.
- Test empty/non-empty get, set, delete operations
